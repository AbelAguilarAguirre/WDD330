let radio = document.querySelector(".hidden");
let packageOption = document.querySelector(".packageOption");
function highlight(e) {
    if(e)
       {e.classList.add("active")}
    else
       return
 }